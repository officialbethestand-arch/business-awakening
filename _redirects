# 覚醒研修LPは当面パーク中（研修モジュール完成時に再構築予定）。
# それまでは Consul Switch（コンサル転職ブログ）へ一時リダイレクト。
# 302=一時的。将来LPを戻す前提のため、恒久(301)にはしない。
# 既存のnote記事等からの流入を404にせず、ブログ（現在の収益源）へ集約する。
# 302! の「!」は強制フラグ。index.html等の実ファイルが存在してもリダイレクトを優先させる
# （Netlifyは既定で実ファイルをリダイレクトより優先するため、! が無いと / が index.html に食われる）。
/*    https://kadomaru-consulting.com    302!
